services:
  - type: web
    name: promptvault-pro
    env: python
    buildCommand: pip install -r requirements.txt && python apk_builder.py
    startCommand: python server.py
    plan: free
    autoDeploy: true
    envVars:
      - key: PORT
        value: 10000
